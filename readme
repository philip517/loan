10 th October 2025
-We made changes to the profile page of the admina to allow the user to make changes to their details as well as their login credentials
-we created the register page to allow users to register or create an account with sefa_satty
-we added a password hashing and verify function to hash the passwords as they are inserted into the database and when logging in
-added the navbar and side panel to appear from the navbar.php file to allow me to recycle the view 
-added the check for email on the register page before creatinng a new user to prevent the use of an already existing email
